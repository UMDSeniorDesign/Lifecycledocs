<HTML>
	<head>
		<script src="../JS/loadXML.js"></script>
		<script src="../JS/saveXML.js"></script>
		<script src="../JS/store.js"></script>
		<script src="../JS/sessionvars.js"></script>


		
		<script>
			function loadPage() {
				
				var text = document.getElementById("text");
				var name = document.getElementById("value");
				
				//var cookiearray = document.cookie.split(';');
				var xmlArray = sessvars.xml.split(';');

				var index = sessvars.value;
				var i = parseInt(index);									/* Changes index from 'string' to 'int' */
				
				//alert(index);
				
				var n = xmlArray[i].split('=')[0];
				var v = xmlArray[i].split('=')[1];
				
				
				name.innerHTML = n;
				text.value = v;
			}
		</script>
		
		<script>
			function update() {												/* Still working on this.. doesn't work yet */
				var text = document.getElementById("text");
				var name = document.getElementById("value");
				
				//var cookiearray = document.cookie.split(';');
				var index = parseInt(cookiearray[(cookiearray.length)-1]);
				
				var value = cookiearray[index].split('=');
				value[1] = text.value;
				cookiearray[index] = value.join('=');
				//document.cookie = cookiearray.join(';');
				document.cookie = cookiearray[index]; 
			}
		</script>
	</head>
	
	<body onload="loadPage()">
		<p id="value"></p>
		<textarea id="text"></textarea>
		<button onclick="update();location.href='view.hta'">Update</button>
		<button onclick=location.href='view.hta'>Back</button>
	</body>
</HTML>